# WingThing Configuration
# Copy this file to config.yaml and fill in your values

discord:
  # Your Discord bot token (from Discord Developer Portal)
  token: "YOUR_DISCORD_BOT_TOKEN"

  # Your Discord user ID (right-click yourself in Discord, Copy ID)
  # Enable Developer Mode in Discord settings to see this option
  user_id: "YOUR_DISCORD_USER_ID"

llm:
  # LLM provider: "anthropic" or "openai"
  provider: "openai"

  # Your API key
  api_key: "YOUR_API_KEY"

  # Model to use for conversations
  # OpenAI: "gpt-5", "gpt-4o", "gpt-4-turbo-preview"
  # Anthropic: "claude-3-5-sonnet-20241022", "claude-3-opus-20240229"
  # Note: Dreams synthesis always uses gpt-5-mini for cost efficiency
  model: "gpt-5"

dreams:
  # Time to deliver Morning Card (24-hour format)
  schedule_time: "07:00"

  # Your timezone (IANA timezone database format)
  timezone: "America/New_York"

database:
  # Path to SQLite database file
  path: "./wingthing.db"

logging:
  # Log level: "debug", "info", "warn", "error"
  # debug: Logs all messages, API requests/responses, timing info
  # info: Logs major events (bot startup, Dreams synthesis, etc)
  # warn: Logs warnings only
  # error: Logs errors only
  level: "debug"

  # Log file path (also outputs to console)
  file: "./wingthing.log"
